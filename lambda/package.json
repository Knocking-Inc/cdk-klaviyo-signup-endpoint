{
  "name": "klaviyo-signup-lambda",
  "version": "1.0.0",
  "description": "Lambda function for Klaviyo email signup",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild index.js --bundle --platform=node --target=node20 --external:@aws-sdk/* --outfile=esbuild-dist/index.js --format=cjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.450.0",
    "@aws-sdk/lib-dynamodb": "^3.450.0",
    "@aws-sdk/client-secrets-manager": "^3.450.0",
    "axios": "^1.6.0"
  }
} 